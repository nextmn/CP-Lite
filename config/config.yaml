control:
  uri: "http://192.0.2.3:8080"
  bind-addr: "192.0.2.3:8080"

slices:
  nextmn-lite:
    pool: "10.0.0.0/24"
    upfs:
      - "203.0.113.1" # only the first upf is used for now

logger:
  level: "debug"
